rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Blurbs are readable by everyone, but only creatable by authenticated users
    match /blurbs/{blurbId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update: if request.auth != null;
    }
    
    // User data is only accessible by the owner
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Chat messages are only accessible by the owner
    match /chats/{userId}/messages/{messageId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Resources are read-only for everyone
    match /resources/{resourceId} {
      allow read: if true;
      allow write: if false;
    }

    // Phrases are read-only for public (search/flashcards)
    match /phrases/{phraseId} {
      allow read: if true;
      allow write: if false;
    }

    // Global Cache is read-only for public
    match /cache_top_phrases/{locale} {
      allow read: if true;
      allow write: if false;
    }
  }
}
